# Archive - AdvancedAttendance

This folder contains an older implementation of the attendance system using:
- PyQt5 for desktop GUI
- FastAPI for web server
- Different architecture

**Note**: This is NOT used by the current system. The main system uses `app.py` (Flask-based).

This is kept for reference only. The current active system is in the root directory.

